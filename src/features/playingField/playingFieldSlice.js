import { createSlice } from '@reduxjs/toolkit';

export const playingFieldSlice = createSlice({
  name: 'playingField',
  initialState: {},
  reducers: {

  },
});

// export const { } = playingFieldSlice.actions;

export default playingFieldSlice.reducer;
